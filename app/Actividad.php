<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Actividad extends Model
{
    // protected $primaryKey = 'id';
    // protected $fillable = ['title', 'description','color','textColor','start','end','user_id'];
}